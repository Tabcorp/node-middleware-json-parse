{
  "name": "middleware-json-parse",
  "version": "1.0.0",
  "description": "JSON parsing middleware",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "TabDigital/node-middleware-json-parse",
  "keywords": [
    "json",
    "parse",
    "middleware",
    "noframework",
    "micro"
  ],
  "license": "MIT",
  "dependencies": {
    "concat-stream": "^1.5.1",
    "is-incoming-message": "^1.0.0",
    "is-server-response": "^1.0.0",
    "noop2": "^2.0.0",
    "safe-json-parse": "^4.0.0"
  },
  "devDependencies": {
    "dependency-check": "^2.5.1",
    "get-server-port": "^1.0.0",
    "istanbul": "^0.4.0",
    "nets": "^3.2.0",
    "standard": "^5.3.1",
    "tape": "^4.2.1"
  },
  "files": [
    "index.js",
    "bin/*"
  ]
}
